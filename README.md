# CT FLIGHT TOOLS

统一机型入口项目。

- CTLS：复制自 `CTLS TOOLS V3.0`
- CTLSi：复制自 `CTLSi TOOLS V2.0`
- 两个源项目未改动。
- CTLS 与 CTLSi 使用不同的本地存储键，注册号、计算草稿、飞行计划和检查单进度相互隔离。
- 根目录 `index.html` 用于选择机型；各机型首页与工具页均可返回机型选择页。

建议通过本地或网络 Web 服务器打开根目录。直接双击 `index.html` 也可使用主要功能，但 PWA 离线缓存仅在 HTTP/HTTPS 环境生效。
